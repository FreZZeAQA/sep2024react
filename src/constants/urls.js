const jsonPlaceHolderUrl = 'https://jsonplaceholder.typicode.com'

const users = '/users'
const comments = '/comments'


const urls = {
    users:{
        base:`${users}`
    },
    comments:{
        base:`${comments}`
    }
}

export {
    urls,jsonPlaceHolderUrl,comments
}